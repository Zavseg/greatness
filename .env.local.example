# Local-only secret for the Vision proxy. Copy to .env.local; NEVER commit .env.local.
VISION_PROXY_TOKEN=replace_with_existing_random_proxy_token

# Public Supabase browser values (not secrets).
SUPABASE_URL=https://fvyqblekphbmbbboitzq.supabase.co
SUPABASE_ANON_KEY=sb_publishable_REPLACE_WITH_PUBLISHABLE_KEY

# Production-only server secrets - configure in Vercel, not browser code.
# SUPABASE_SECRET_KEY=NEVER_COMMIT_THIS_VALUE
# GAS_SERVICE_TOKEN=NEVER_COMMIT_THIS_VALUE
